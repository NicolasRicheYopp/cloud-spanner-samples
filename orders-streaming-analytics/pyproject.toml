[project]
name = "gcp-spanner-boost-streaming-demo"
version = "0.1.0"
description = "Spanner Data Boost streaming demo, driven end-to-end from demo.ipynb"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyterlab>=4.0",
]
